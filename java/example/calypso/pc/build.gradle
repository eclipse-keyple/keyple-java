apply plugin: 'java'

sourceCompatibility = 1.6

project.version = '0.7.0'+'-'+ timestamp+ '-SNAPSHOT'

jar {
    manifest {
        attributes 'Implementation-Title': 'Keyple Example Pc',
                'Implementation-Version': project.version
    }
}

dependencies {
    //use keyple library : calypso
    implementation project(path:':component:keyple-calypso')

    //use keyple library : core
    implementation project(path:':component:keyple-core')

    //use keyple library : plugin stub
    implementation project(path:':component:keyple-plugin:keyple-plugin-stub')

    //use keyple library : plugin pcsc
    implementation project(path:':component:keyple-plugin:keyple-plugin-pcsc')

    //use keyple example : generic-common
    implementation project(path: ':example:generic:example-generic-common')

    //use keyple example : generic-pc
    implementation project(path: ':example:generic:example-generic-pc')

    //use keyple example : calypso common
    implementation project(path: '::example:calypso:example-calypso-common')

    //https://mvnrepository.com/artifact/org.slf4j/slf4j-simple
    implementation "org.slf4j:slf4j-simple:${slf4jsimple_version}"
    implementation "org.slf4j:slf4j-ext:${slf4jext_version}"

}
